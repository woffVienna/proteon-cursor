SERVICE_NAME=identity-service
ENV=prod
MARKET=AT
VERSION=dev

# Keep keys consistent with local/docker.
# In cloud, values are usually injected by deployment tooling/secrets.
PORT=8081
PUBLIC_BASE_URL=http://localhost:8081
JWT_ISSUER=proteon.identity
JWT_AUDIENCE=proteon-api
